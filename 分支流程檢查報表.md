# 分支流程與輸入/輸出檢查報表

> ⚠ 歷史文件註記(2026-07-25):本檔 Chart-GCN 部分已被 `實驗記錄_論文對齊.md`(2026-07-24 全流程審查)取代;
> 第四章融合方案已由 `融合實驗報告.md` 實證推翻(V1~V7c 皆未達標)。
> 保留原因:main 分支審查(M-1~M-4)與 C 系列問題原始定義的唯一出處、週進度計畫 W1 交付物。

檢查日期：2026-07-04
Repo：https://github.com/lintaixu/fcukproject.git
檢查範圍：`main` 分支（K線型態分析系統）與 `Chart-GCN` 分支（Chart GCN 論文復現）之核心程式碼。

- ✅ = 輸入/輸出銜接正確
- 🟡 = 有不一致但不會直接出錯（文件、語意、邊界問題）
- 🔴 = 輸入/輸出對不攏或方法學錯誤，會影響結果正確性

---

## 一、main 分支

### 1.1 整體流程

只有一支主程式 `kline_pattern_search.py`（892 行，Tkinter GUI）：

```
GUI 按鈕①  download_raw()          ── yfinance 下載 50 檔台股 2015~2025 (auto_adjust=False)
                │  {ticker: DataFrame[Open,High,Low,Close,Adj Close,Volume]}
                ▼
GUI 按鈕②/③  build_dataset(raw, feat_fn)   ── 對三種特徵集各跑一次
                │  DataFrame[特徵欄位…, future_ret, ticker]（已剔除除權息日、NaN）
                ▼
   ┌──────────②特徵驗證────────────┐   ┌──────────③分析+回測──────────────┐
   │ validate_features(df, cols)    │   │ find_and_train(df, cols)          │
   │  → AE 壓縮 → OCSVM             │   │  → Euclidean 找相似型態群         │
   │  → in-sample 混淆矩陣 metrics  │   │  → 重要性權重 → AE → OCSVM        │
   └────────────────────────────────┘   │ backtest(df, cols, models, 股票)  │
                                        │  → 2025 年單檔訊號回測            │
                                        └───────────────────────────────────┘
                ▼
        GUI 繪圖（混淆矩陣 / 累積報酬曲線）
```

### 1.2 各函式輸入/輸出

| 函式 | 輸入 | 輸出 | 範例 | 狀態 |
|---|---|---|---|---|
| `_download_one(ticker)` | 股票代號 str | `(ticker, DataFrame)` 或 `(ticker, None)`；欄位需含 Open/High/Low/Close/Volume/Adj Close | `('2330.TW', df)`，df 約 2600 列，index 為日期 | ✅ |
| `download_raw(log_fn, progress_fn)` | 兩個 callback | `dict {ticker: DataFrame}` | 約 50 檔、每檔 ~2600 列原始 OHLCV | ✅ |
| `detect_exdiv_mask(df)` | 含 Adj Close 與 Close 的 df | `pd.Series[bool]`（除權息日 True） | 台積電一年約 4 個 True | 🟡 見問題 M-3 |
| `features_1day(df)` | 原始 OHLCV df | `DataFrame (T, 7)`：upper/lower/body/gap/close_chg/vol_ratio/trend | 2330 一列：`upper=0.42, body=1.13, trend=2.5…`（單位 %） | ✅ |
| `features_2day(df)` | 同上 | `DataFrame (T, 10)`（加前一日 p_upper/p_lower/p_body） | 同上格式 | ✅ |
| `features_3day(df)` | 同上 | `DataFrame (T, 12)`（加 p1/p2 body、cum_return、range_pos…） | `range_pos=73.2` 表示收盤在 3 日區間 73% 高度 | ✅ |
| `build_dataset(raw, feat_fn)` | 原始資料 dict + 特徵函式 | 多股票縱向 concat 的 DataFrame：特徵欄 + `future_ret`（未來 5 日報酬%）+ `ticker`；已去除除權息列與 NaN | 約 120,000 列 × (7~12+2) 欄；`future_ret=+3.42` | ✅ |
| `train_ae(X, input_dim, log_fn)` | `np.ndarray (n, F)` 標準化後特徵 | 訓練好的 `Autoencoder`（encoder 輸出 latent 4 維） | 輸入 (8500, 7) → encode 後 (8500, 4) | ✅ |
| `find_and_train(df, feat_cols, log_fn)` | 全樣本 df + 特徵欄名 | `dict {'scaler': StandardScaler, 'bull': {'ae','ocsvm'} 或 None, 'bear': …}` | 看漲種子 3,200 個 → 有效型態群 41 → AE+OCSVM | ✅ |
| `backtest(df, feat_cols, models, ticker)` | 全樣本 df、models、單一股票代號 | `dict`：signal 陣列(+1/-1/0)、n_buy、n_sell、win_rate、cum_ret… | `{'n_buy': 12, 'win_rate': 0.58, 'cum_ret': +8.3}` | 🔴 見問題 M-1 |
| `validate_features(df, feat_cols, log_fn)` | 同上 | `dict {'bull': {TP,TN,FP,FN,precision,recall,accuracy,f1}, 'bear': …}` | `precision=0.31, recall=0.62` | 🟡 見問題 M-2 |

### 1.3 main 分支問題清單

> **2026-07-04 更新：M-1 ~ M-4 已全部修正，commit `2055f71`（main 分支）。**
> - M-1：回測改為非重疊持倉（進場後 HOLD_DAYS 日內忽略新訊號），回傳值新增 `raw_signal` 保留原始訊號
> - M-2：`validate_features` 改為訓練期內依日期 80/20 切分、後 20% 做 out-of-sample 驗證，評估門檻改與訓練種子一致（RISE/FALL_THRESH）
> - M-3：GUI 補充下載改用 `_download_one()`，必要欄位（含 Adj Close）檢查生效
> - M-4：vol_ratio 分母改為 v5ma / v3ma（常規量比定義）

**🔴 M-1｜回測重疊持倉，cum_ret 高估（backtest, 411~424 行）**
每個交易日都可能出訊號，每筆訊號的報酬用 `future_ret`（未來 **5 日**報酬）計算，然後 `cum_ret = act.sum()` 直接加總。
→ 相鄰兩天都出買訊時，兩筆 5 日持有期重疊，同一段漲幅被計算多次。這不是真實逐日淨值回測，累積報酬與勝率都會失真（同方向連續訊號時系統性高估）。

**🟡 M-2｜validate_features 是 in-sample 評估，且門檻不一致（439~504 行）**
- AE 與 OCSVM 用「訓練期 2016–2024」資料訓練，混淆矩陣也是對同一批訓練資料算 → 是訓練集自我評估，不是驗證集表現，數字會偏樂觀。
- OCSVM 用 `y > 5%`（RISE_THRESH）的種子訓練，但評估的正例定義是 `y > 2%`（MIN_BULL_RET）→ 訓練目標與評估目標的「正例」定義對不攏，precision/recall 解讀時要註明。

**🟡 M-3｜GUI 補充下載路徑缺 Adj Close 檢查（_worker_run, 743~752 行）**
`_download_one()` 有檢查六個必要欄位（含 Adj Close），但使用者輸入不在 TW50 清單的代號時，`_worker_run` 補下載只檢查 `len(df) < 100`，沒檢查 Adj Close。若缺欄位，`detect_exdiv_mask` 靜默回傳全 False → 該股票的除權息日不會被剔除，特徵混入除息跳空的假訊號。

**🟡 M-4｜vol_ratio 分母用當日量而非均量（100 行）**
`vol_ratio = (V - v5ma) / V`，一般定義是除以 v5ma。目前寫法值域不對稱（爆量日被壓縮、縮量日被放大），不算錯但屬非常規定義，比較特徵集時要留意。

---

## 二、Chart-GCN 分支

### 2.1 整體流程

```
main.py / run_final.py 等腳本
   │
   ▼
core/data_loader.py  fetch_tw_stocks()
   │  {ticker: DataFrame[open,high,low,close,volume]}  (yfinance auto_adjust=True, parquet cache)
   ▼
core/dataset.py  split_by_date() → (train_dict, test_dict)
   │
   ▼
core/dataset.py  ChartGCNDataset  ── 對每檔股票、每個 window 滑窗（多進程並行）:
   │    ├─ core/indicators.py   compute_indicators(df, n)      → (T, 9) 指標矩陣
   │    ├─ core/pip_algorithm.py extract_pips(series, m)       → (indices, scores)
   │    ├─ core/vg_graph.py     build_visibility_graph(...)    → networkx.Graph
   │    └─ core/subgraph.py     build_3d_feature(...)          → X (N, g, 9)
   │  Dataset：X (樣本數, N, g, 9)、y (樣本數,)、meta [(ticker, date)]、date_to_indices
   ▼
core/train.py  fit()  ── DateGroupedBatchSampler（同日股票同 batch）
   │    core/model.py ChartGCN：(B,N,g,9) → Conv×3 → FC → 跨樣本 Self-Attention → logits (B,2)
   │  回傳 (model, test_metrics)
   ▼
backtest.py  run_backtest() → 逐股買賣模擬 vs 等權 benchmark → plot_backtest() 淨值圖
```

### 2.2 各檔案輸入/輸出

| 檔案/函式 | 輸入 | 輸出 | 範例 | 狀態 |
|---|---|---|---|---|
| `data_loader.fetch_tw_stocks(tickers, start, end)` | 代號清單、日期字串 | `dict {ticker: DataFrame[open,high,low,close,volume]}`，index 為日期 | `data['2330.TW']` → 1,700 列，`close=593.0` | ✅ |
| `indicators.compute_indicators(df, n)` | df 需含 open/high/low/close 欄；n=指標窗口 | `np.ndarray (T, 9) float32`，逐欄 z-score | 輸入 970 列 df、n=100 → `(970, 9)`，每欄 mean≈0, std≈1 | 🔴 見問題 C-3 |
| `pip_algorithm.extract_pips(series, m)` | 1D `np.ndarray` 長度 n（一個 window 的收盤價）、m 個關鍵點 | `(indices: List[int] 長度 m 已排序, scores: np.ndarray (n,))` | 輸入 (100,)、m=40 → indices 長度 40、scores 非 PIP 處為 0 | ✅ |
| `vg_graph.build_visibility_graph(series, pip_indices)` | 完整 series + PIP index 清單 | `networkx.Graph`，節點 0..m-1，屬性 time/price | 40 個節點、約 80~120 條邊 | ✅ |
| `subgraph.select_top_nodes(G, scores, N)` | 圖、importance 分數、N | 前 N 個節點 id 清單 | N=15 → `[0, 39, 17, …]` | ✅ |
| `subgraph.bfs_subgraph(G, root, g)` | 圖、根節點、子圖大小 g | `List[(node_id, depth)]` 最多 g 個 | g=5 → `[(17,0),(16,1),(18,1),(15,2),(19,2)]` | ✅ |
| `subgraph.normalize_subgraph(G, sub, scores, g)` | BFS 結果 | **實際回傳 2 個值** `(ordered_nodes, adj)` | ordered 長度 5（不足補 -1）、adj (5,5) | 🟡 見問題 C-6 |
| `subgraph.build_3d_feature(...)` | series、pips、scores、(T,9) 指標、G、N、g | `np.ndarray (N, g, 9) float32` | `(15, 5, 9)` | ✅ |
| `dataset.ChartGCNDataset(stock_data, window, m_pips, N, g, stride…)` | 股票 dict + 超參數 | Dataset：`__getitem__` → `(X: Tensor (N,g,9), y: Tensor 0/1)`；另有 `.X .y .meta .date_to_indices` | 50 檔×970 天、stride=1 → 約 43,000 樣本 | 🟡 見問題 C-4 |
| `dataset.split_by_date(stock_data, train_end)` | dict + 切分日 | `(train_dict, test_dict)` | train ≤2023-12-31、test 2024 年 | ✅ |
| `model.ChartGCN.forward(X)` | `(B, N, g, 9)` Tensor | `logits (B, 2)` | `(37, 15, 5, 9)` → `(37, 2)`；B=同日股票數 | ✅ |
| `train.DateGroupedBatchSampler(dataset)` | Dataset 或 random_split 後的 Subset | 每次 yield 同一交易日的樣本 index 清單 | 一個 batch = 某日的 37 檔股票 | ✅（有處理 Subset） |
| `train.fit(train_ds, val_ds, test_ds, …, batch_size=64, …)` | 三個 dataset + 超參數 | `(model, test_metrics dict)` | `{'acc':0.515, 'f1_1':0.373, …}` | 🔴 見問題 C-1 |
| `backtest.get_per_stock_predictions(model, test_ds)` | 模型 + **完整** ChartGCNDataset（不能是 Subset，需 `.X .meta`） | `dict {ticker: [(date, 0/1), …]}` | `{'2330.TW': [(2024-06-05, 1), …]}` | ✅（main.py 傳入的確是完整 dataset） |
| `backtest.simulate_one_stock(ticker, preds, price_df, indicator_n)` | 預測序列 + 價格 df | `dict`：dates、net_values、n_trades、total_return | `{'final_nv': 1.043, 'n_trades': 18}` | 🔴 見問題 C-5 |
| `backtest.run_backtest(model, test_ds, stock_data, indicator_n)` | 模型、測試集、測試期價格 dict | `dict`：stock_results、avg_net_values、benchmark_nv… | 策略平均淨值 0.942 vs benchmark 0.961 | 🟡 見問題 C-7 |
| `test/main.py`（工作區已搬移，commit 中在根目錄） | CLI 參數 | 單次訓練或兩階段 Grid Search + 回測 + png | `python main.py --grid-search` | 🔴 見問題 C-2 |

### 2.3 Chart-GCN 分支問題清單

> **2026-07-06 更新：C-1 ~ C-7 已全部修正（工作區，未 commit），並通過 end-to-end smoke test。**
> - C-1：`fit()` 移除 batch_size 參數，`main.py` 移除 --batch-size（batch 由 DateGroupedBatchSampler 決定）
> - C-2：`fit()` 回傳 `val_f1_macro`，grid search 改以驗證集 macro-F1 排序選參
> - C-3：`compute_indicators` 支援注入統計量；測試集沿用訓練集 `norm_stats`
> - C-4：`split_by_date` 加 `warmup_rows`（帶 2×window 歷史），dataset 加 `min_date` 過濾 —— 測試期全年皆有樣本（修正前僅下半年）
> - C-5：meta 日期改為決策日（window 最後一天），回測交易時點與 label 定義一致
> - C-6：`normalize_subgraph` docstring 修正（BFS 截斷取代論文 filter-out，不回傳 valid）
> - C-7：回測平均淨值與 benchmark 改以日期軸對齊（ffill 後逐日平均）
> - C-8：維持 sys.path shim 設計（可直接執行單檔的取捨），已於文件註明

**🔴 C-1｜`fit()` 的 `batch_size` 參數完全沒被使用（train.py:106, main.py:81）**
`fit()` 簽名收 `batch_size=64`，`main.py` 也認真傳了 `--batch-size`(預設 128)，但三個 DataLoader 全部用 `batch_sampler=DateGroupedBatchSampler(...)`，batch 大小實際由「當日股票數」決定。**輸入介面對不攏**：使用者調 `--batch-size` 完全無效且無任何警告。應移除該參數或印出警告。

**🔴 C-2｜Grid Search 用『測試集』選超參數（main.py grid_search）**
`fit()` 回傳的 `metrics` 是 **test set** 上的分數（用 val 選最佳 epoch 權重後在 test 上算），而 grid search 的 `all_results.sort(key=f1_macro)` 直接拿這個 test f1_macro 挑最佳參數，最後又用同一個 test set 報最終成績 → 測試集資訊洩漏，`best_params.json` 裡的 0.515 分數偏樂觀。應改用 **validation** f1_macro 選參。

**🔴 C-3｜indicators.py z-score 使用全期間 mean/std → 前視偏誤（indicators.py:85-87）**
`compute_indicators` 對整段 df（訓練集為 2018~2023 全部）算 mean/std 再正規化，之後才切滑窗。也就是 2019 年某個 window 的特徵值，含有 2023 年資料算出的統計量 → look-ahead bias。且 train 與 test 各自用自己的 mean/std 正規化，**兩邊尺度不一致**（模型在 train 分佈學的權重，test 輸入分佈不同）。正確作法：只用訓練期統計量，或在每個 window 內部正規化。

**🔴 C-5｜回測交易時點與 label 定義對不攏（dataset.py:77 vs backtest.py:94-113）**
- label 定義：`close[end] > close[end-1]`，特徵只用到第 end-1 天 → 模型預測的是「第 end 天的漲跌」。
- 但回測在 meta date（= 第 end 天）用 **當天收盤價** 買入。此時預測的那根 K 棒已經走完 —— 預測「今天會漲」，卻在今天收盤才買，賺到的是「明天以後」的行情，而不是模型被訓練去預測的那一天。
→ 沒有未來函數（不會偷看），但**策略報酬與模型預測目標脫鉤**，回測結果無法直接反映分類能力。建議 label 改為 `close[end+1] > close[end]`（預測隔日）或回測改為次日開盤成交。

**🟡 C-4｜測試集指標/滑窗沒有 warm-up 歷史（dataset.py + main.py 資料切分）**
`split_by_date` 硬切後，test dict 只含 2024 年資料，於是：
1. `compute_indicators(test_df, n=130)`：2024 年初的指標在 `min_periods=1` 下用極少樣本算出，跟訓練期同名指標意義不同；
2. 滑窗要滿 `window`(100~140) 天才產生第一個樣本 → 2024 全年 ~245 個交易日只剩約 105~145 天有預測，回測期實際只有下半年。
建議建 test dataset 時帶入訓練期最後 window+n 天作 warm-up，再丟棄落在訓練期的樣本。

**🟡 C-6｜normalize_subgraph 文件與實作不符（subgraph.py:60-107）**
docstring 說回傳 `(ordered_nodes, adj, valid)` 三個值且「節點 > g 過濾該子圖」，實際只回傳 2 個值，論文的 filter-out 邏輯沒實作（BFS 已截斷到 g 個所以不會爆，呼叫端也只解包 2 個）。文件需更新，或補實作過濾。另外 `build_3d_feature` 算出的 `adj` 直接被丟棄（`_adj`）——模型已移除 GCN 層所以是刻意的，但值得在 docstring 註明。

**🟡 C-7｜回測平均淨值以位置 index 對齊，不是日期對齊（backtest.py:170-176, 234-241）**
各股票的 `net_values` 長度不同（除牌、停牌、資料缺漏），`min_len` 截斷後按「第 i 個交易日」平均 —— 不同股票的第 i 個元素可能是不同日期。benchmark 也是同樣的 index 對齊。誤差通常小，但嚴謹作法是先 reindex 到共同日期軸。

**🟡 C-8｜工作區結構與 import 方式脆弱**
- 未提交變更把 `main.py`、`run_*.py` 移到 `test/`，靠 `sys.path.insert` shim 找到 `core/` 與根目錄的 `backtest.py`。
- `core/` 內部用 flat import（`from pip_algorithm import …`），因此 **無法** `from core.dataset import …` 直接匯入，只能透過 shim。若日後要當 package 用，需改成 relative import（`from .pip_algorithm import …`）並加 `__init__.py`。
- `dataset.py:126` 空集 fallback `np.zeros((0, N, g, 9))` 硬編碼 F=9，若指標數改變會與模型 `F_dim` 對不上。

---

## 三、兩分支對照總結

| 面向 | main（K線型態系統） | Chart-GCN（論文復現） |
|---|---|---|
| 方法 | Euclidean 相似型態挖掘 → AE 壓縮 → One-Class SVM | PIP → Visibility Graph → 子圖正規化 → CNN + 跨股票 Self-Attention |
| 資料 | yfinance 50 檔，2015–2025，**未還原**（auto_adjust=False，自行剔除除權息日） | yfinance 50/100 檔，2018–2024，**已還原**（auto_adjust=True），parquet cache |
| 標籤 | 未來 5 日報酬 %（連續值，回測直接用） | 隔日(第 end 天)漲跌二分類 |
| 訓練/測試切分 | 2016–2024 / 2025 | ≤2023-12-31 / 2024 |
| 介面 | Tkinter GUI，單檔程式 | CLI 腳本 + core/ 模組 |
| 最嚴重問題 | 回測重疊持倉高估報酬 (M-1)（已修正 `2055f71`） | Grid search 用測試集選參 (C-2)、z-score 前視偏誤 (C-3)、回測時點錯位 (C-5) |

### 修正優先順序建議（Chart-GCN）
1. C-2：grid search 改用 validation f1_macro 選參。
2. C-3：z-score 改用訓練期統計量（或 window 內正規化），train/test 用同一組統計量。
3. C-5：統一 label 與交易時點（建議預測 t+1、以 t+1 開盤或收盤成交）。
4. C-1：移除無效的 batch_size 參數。
5. C-4：test dataset 加 warm-up 歷史。

---

## 四、兩方法融合可行性評估（提升準確度）

### 4.1 兩方法的互補性

| | 系統 A（main：K線型態 + AE + OC-SVM） | 系統 B（Chart-GCN） |
|---|---|---|
| 視野 | 短期（1~3 日 K 棒形態） | 長期（100~140 日圖表結構） |
| 預測目標 | 未來 **5 日** 是否大漲/大跌（>±5%） | **隔日** 漲跌二分類 |
| 輸出特性 | 稀疏、高選擇性訊號（大多數日子無訊號） | 每日都有預測、目前準確度僅 ~51.5%（接近隨機） |
| 擅長 | 找「罕見但可靠」的局部型態、precision 導向 | 跨股票市場結構（同日 attention）、recall 導向 |

兩者時間尺度與訊號密度天然互補 → 融合可行，且有多個切入層級。

### 4.2 前置條件（不做，融合效果無法衡量）

1. **先修 C-2 / C-3 / C-5**：目前 B 的 51.5% 準確度含測試集選參與前視偏誤，基準線本身不可信，任何融合的「提升」都量不出來。
2. **統一標籤視野**：建議把 B 的 label 從「隔日漲跌」改為「未來 5 日報酬方向」（與 A 的 HOLD_DAYS=5 對齊）。隔日漲跌雜訊極大（本質上接近 50/50），5 日視野訊噪比較好，也是 B 準確度可能提升的最大單一因素。
3. **統一資料源**：A 用未還原價+剔除除權息日、B 用還原價。融合時一律用還原價（K 棒比例特徵不受還原影響），並沿用 A 的除權息日剔除邏輯。

### 4.3 融合方案（依成本由低到高）

**方案一｜訊號層 AND 閘門（建議先做，1~2 天工作量）**
B 每日輸出漲跌機率 → 只有當 A 的 OC-SVM 同時判定「當日形態屬於看漲型態群」才進場（看跌同理）。
- 不需重新訓練任何模型，直接在回測層把兩個訊號 AND 起來。
- 預期效果：交易筆數大減、**每筆訊號 precision 上升**（A 過濾掉 B 的低置信度雜訊）。
- 衡量指標：訊號 precision、勝率、扣成本後累積報酬 vs 各自單獨跑。

**方案二｜特徵層融合（中成本，最符合現有架構）**
把 A 的 K 棒特徵接進 B 的節點特徵：每個 PIP 節點目前帶 9 個技術指標，再附加該日的 1 日 7 維（或 3 日 12 維）K 棒特徵 → F 從 9 → 16/21 維。
- 只改 `indicators.py`（加特徵欄）與 `F_dim` 參數，pipeline 其他部分不動（Conv1 kernel = (g, F) 自動適應）。
- 可搭配 **GA 特徵挑選**：以 GA 的疊代/突變在擴充後的特徵池中搜尋最佳子集（適應度 = validation macro-F1），並與原始特徵集做對照實驗 —— 這與你進度報告的 GA 主軸直接銜接。
- 風險：K 棒特徵是短期訊號，掛在長期 PIP 節點上可能只有 window 尾端幾個節點受益；可只對最後 k 個 PIP 節點附加。

**方案三｜樣本加權（低~中成本）**
用 A 的型態挖掘結果替 B 的訓練樣本加權：window 結尾落在「有效型態群」日期的樣本，用 A 算出的 importance（freq × |avg_ret|）作為 `CrossEntropyLoss` 的 sample weight。
- 讓 B 把容量花在「歷史上有跡可循」的樣本，弱化純雜訊日。

**方案四｜雙分支架構（高成本，前三者有效後再考慮）**
Chart-GCN 分支（長期結構）+ MLP 分支（近 3 日 K 棒特徵 / A 的 AE latent 4 維向量），在 FC 層 concat 後再進 Self-Attention。
- 效果上限最高，但需重訓與重新調參，且在基準線修正前不值得投入。

### 4.4 建議路線

```
步驟 0  修 C-2/C-3/C-5，label 改 5 日視野     ← 重建可信基準線
步驟 1  方案一（訊號 AND 閘門）               ← 一週內可出對照數據
步驟 2  方案二（特徵融合 + GA 特徵挑選）      ← 與進度報告 GA 主軸整合
步驟 3  視結果決定是否做方案三/四
```

評估協定：所有比較都用 validation 選參/選模型、test 只跑一次；同時報告分類指標（macro-F1）與交易指標（訊號 precision、扣成本報酬），因為融合的主要價值預期出現在**交易指標**上。
